# Component inventory

The project uses custom React/Tailwind components and no external UI component library.

## Shared primitives

No standalone primitive directory exists. Reusable patterns are implemented in the layout and view components using Tailwind utility classes.
